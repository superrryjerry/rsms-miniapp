.activity-header { display: flex; align-items: center; margin-bottom: 8rpx; }
.method { font-size: 24rpx; color: #666; margin-left: 12rpx; }
.customer-name { display: block; font-size: 30rpx; font-weight: bold; margin-bottom: 8rpx; }
.activity-preview { display: block; font-size: 26rpx; color: #666; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.time { display: block; font-size: 22rpx; color: #999; margin-top: 8rpx; }
.action-row { display: flex; gap: 16rpx; margin-top: 12rpx; }
.action-btn { padding: 8rpx 32rpx; border-radius: 8rpx; font-size: 24rpx; }
.action-btn.edit { background: #e6f7ff; color: #1890ff; border: 1rpx solid #91d5ff; }
.action-btn.delete { background: #fff1f0; color: #ff4d4f; border: 1rpx solid #ffa39e; }
/* 底部悬浮新增按钮 */
.fab-btn {
  position: fixed;
  left: 50%;
  bottom: calc(60rpx + env(safe-area-inset-bottom));
  transform: translateX(-50%);
  z-index: 100;
}
.fab-circle {
  width: 100rpx;
  height: 100rpx;
  background: linear-gradient(135deg, #1890ff, #096dd9);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 8rpx 24rpx rgba(24, 144, 255, 0.4);
}
.fab-icon {
  font-size: 56rpx;
  color: #fff;
  font-weight: 300;
  line-height: 1;
}
.loading-tip { text-align: center; padding: 20rpx; color: #999; font-size: 24rpx; }
